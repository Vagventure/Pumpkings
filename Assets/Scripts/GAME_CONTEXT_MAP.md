# Game Context Map Compatibility Pointer

The canonical intent-based context router is [root `CONTEXT-MAP.md`](../../CONTEXT-MAP.md). Start with [root `CONTEXT.md`](../../CONTEXT.md), then select only the focused documents relevant to the task.

This file remains only so existing Unity assets, links, or agent workflows do not break. Do not add a second routing hierarchy here.
